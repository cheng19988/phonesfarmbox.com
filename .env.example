# SQLite database (local dev). On Vercel, leave unset — runtime copies prisma/data.db to /tmp.
DATABASE_URL="file:./prisma/data.db"

# JWT secret for auth sessions (set a long random string in production)
AUTH_SECRET="change-me-in-production"
